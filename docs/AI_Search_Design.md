# 🏛️ การออกแบบระบบ AI Search Engine สำหรับ Sim-All.com

## 1. ภาพรวมสถาปัตยกรรม (Architecture Overview)

ระบบ AI Search Engine ถูกออกแบบมาให้ทำงานบนสถาปัตยกรรม 3 ชั้น (Three-Layered Architecture) เพื่อให้สามารถจัดการกับข้อมูลเชิงศาสตร์ตัวเลข (Numerology) และตอบคำถามเชิงความหมาย (Semantic Query) ได้อย่างแม่นยำ



### 1.1 Ingestion Layer (ชั้นการดึงข้อมูล)
* **หน้าที่:** ดึงและจัดเก็บข้อมูลดิบจากเว็บไซต์
* **เครื่องมือ:** `src/scraper.py`
* **ผลลัพธ์:** ข้อมูล JSON/HTML ดิบใน `data/raw/`

### 1.2 Processing & Modeling Layer (ชั้นการประมวลผลและโมเดล)
* **หน้าที่:** ทำความสะอาดข้อมูล, สร้าง Dataset, และฝึกโมเดล AI
* **เครื่องมือ:** `src/numerology_parser.py`, Jupyter Notebooks (`notebooks/`)
* **โมเดลหลัก:**
    * **Intent Classifier:** โมเดลจำแนกเจตนา (TOOL vs. INFO)
    * **Semantic Encoder (Embeddings):** โมเดลภาษาไทย (เช่น Thai BERT) สำหรับการแปลงข้อความบทความเป็น Vector

### 1.3 Retrieval Layer (ชั้นการค้นหาและตอบคำถาม)
* **หน้าที่:** รับ Query จากผู้ใช้, ทำนายเจตนา, ค้นหาคำตอบที่เกี่ยวข้อง, และส่งผลลัพธ์กลับ
* **เครื่องมือ:** `src/ai_model.py`
* **ฟังก์ชันหลัก:** การตัดสินใจว่าจะส่งผู้ใช้ไปยัง **เครื่องมือ (Tool)** หรือ **คำตอบเชิงสังเคราะห์ (Synthesized Answer)**

## 2. การจัดการเจตนาของผู้ใช้ (Intent Classification Strategy)

ระบบต้องสามารถแยกแยะความต้องการของผู้ใช้ได้อย่างชัดเจน เพื่อมอบประสบการณ์ที่ตรงจุด:

| Intent Class | ตัวอย่าง Query | การจัดการผลลัพธ์ | Priority |
| :--- | :--- | :--- | :--- |
| **`TOOL_CHECK_NUMBER`** | "เช็คเบอร์ 08xxxxxxx", "ทำนายเบอร์ใหม่" | Redirect ไปยังหน้าเครื่องมือวิเคราะห์เบอร์โทรศัพท์ | **High** |
| **`INFO_NUM_MEANING`** | "ความหมายเลข 42", "เลข 63 ดีไหม" | ใช้ Semantic Search ดึงบทความและสร้างคำตอบสังเคราะห์จาก `numerology_meaning.csv` | Medium |
| **`INFO_TIMING_HORO`** | "ดวงปีหน้า", "ฤกษ์มงคลธันวา" | ใช้ Temporal Filter ค้นหาบทความที่อัปเดตล่าสุดตามช่วงเวลา | Medium |
| **`INFO_LUCKY_NUMBER`** | "เลขเด็ดงวดนี้", "ทำนายฝัน" | ค้นหาบทความหวยล่าสุด (เน้นวันที่ออกรางวัล) | Low |

## 3. กลไกการค้นหาเชิงความหมาย (Semantic Retrieval Mechanism)

1.  **Indexing:** เนื้อหาบทความทั้งหมดจะถูกแปลงเป็น **Vector Embeddings** โดยใช้โมเดลภาษาไทย (e.g., WangchanBERTa) และเก็บไว้ใน Vector Database
2.  **Query Matching:** เมื่อผู้ใช้ป้อนคำถาม (Query) จะถูกแปลงเป็น Vector เช่นกัน
3.  **Similarity Search:** คำนวณระยะห่างทางคณิตศาสตร์ (Cosine Similarity) ระหว่าง Query Vector กับ Article Vectors เพื่อหาบทความที่มี **ความหมาย** ใกล้เคียงที่สุด แม้คำจะไม่ตรงกันก็ตาม

## 4. E-E-A-T & AEO Integration

เพื่อให้ AI Search Engine (เช่น Google's AI Overviews) เชื่อถือข้อมูลของเรา:

* **Citation Focus:** ผลลัพธ์ที่สังเคราะห์ต้องอ้างอิงถึง URL ต้นทางเสมอ
* **Structured Output:** ใช้ข้อมูลจาก `schema_markup_samples/` ในการตอบคำถามที่เป็นตารางหรือขั้นตอน (e.g., How to Check Your Number Score) เพื่อความชัดเจน
